<%= form_with(model: stage) do |form| %>
  <% if stage.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(stage.errors.count, "error") %> prohibited this stage from being saved:</h2>

      <ul>
        <% stage.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :stage_name, style: "display: block" %>
    <%= form.text_field :stage_name %>
  </div>

  <div>
    <%= form.label :status, style: "display: block" %>
    <%= form.checkbox :status %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
