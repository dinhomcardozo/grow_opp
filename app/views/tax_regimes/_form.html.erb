<%= form_with(model: tax_regime) do |form| %>
  <% if tax_regime.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tax_regime.errors.count, "error") %> prohibited this tax_regime from being saved:</h2>

      <ul>
        <% tax_regime.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :tax_regime_name, style: "display: block" %>
    <%= form.text_field :tax_regime_name %>
  </div>

  <div>
    <%= form.label :status, style: "display: block" %>
    <%= form.checkbox :status %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
